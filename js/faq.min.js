const items=document.querySelectorAll(".c-accordion button");function toggleAccordion(){let t=this.getAttribute("aria-expanded");for(i=0;i<items.length;i++)items[i].setAttribute("aria-expanded","false");"false"==t&&this.setAttribute("aria-expanded","true")}items.forEach(t=>t.addEventListener("click",toggleAccordion));